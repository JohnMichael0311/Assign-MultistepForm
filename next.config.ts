import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "export",  // Set this for static export
  // Add any other Next.js configuration options if needed
};

export default nextConfig;
